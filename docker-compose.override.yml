services:
  strapi:
    build:
      target: development
    container_name: strapi_dev
    working_dir: /app
    volumes:
      - ./:/app
      - /app/node_modules
    ports:
      - "1337:1337"
    environment:
      - CHOKIDAR_USEPOLLING=true