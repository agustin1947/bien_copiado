/**
 * cuenta-corriente controller
 */

import { factories } from '@strapi/strapi'
import type { UID } from '@strapi/types';

export default factories.createCoreController('api::cuenta-corriente.cuenta-corriente' as UID.CollectionType);
